# EC2 System Monitor

A lightweight Java 17 HTTP monitoring service that runs directly on an AWS EC2 Linux instance and exposes structured system, JVM, process, CPU, memory, and filesystem metrics via a REST-style HTTP API.

## Project Identity
- **GroupId**: `com.shevay`
- **ArtifactId**: `ec2-system-monitor`
- **Version**: `1.0-SNAPSHOT`
